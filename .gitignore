# 依赖
node_modules/
package-lock.json
yarn.lock

# 运行时配置（含本机绝对路径，不入库）
server/data/config.json
server/data/
.vercel/

# 备份的临时写入文件（原子写的中间态，正常不会残留）
server/data/backups/**/*.tmp
server/data/backups/*.tmp

# 日志与临时文件
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# 环境变量
.env
.env.local

# 编辑器
.vscode/
.idea/

# AI 助手本地状态
.workbuddy-ai/
